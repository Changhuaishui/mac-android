# M1 Protocol 解耦验证资产约定 - 验证说明

## 基本信息

- 日期：2026-06-30
- 主 Agent：Codex（M1 Protocol Agent）
- 任务卡：`任务记录/2026-06-30-M1-Protocol-解耦验证资产约定.md`
- 范围：`测试资产/M1-解耦验证/` 下的 protocol frame、replay server、dump client 和 `sample-annexb.h264` 生成约定。

## 交付内容

- 新增 `测试资产/M1-解耦验证/protocol-frame-example.md`。
- 新增 `测试资产/M1-解耦验证/replay-server-notes.md`。
- 新增 `测试资产/M1-解耦验证/dump-client-notes.md`。
- 新增 `测试资产/M1-解耦验证/sample-annexb-generation.md`。
- 更新 `测试资产/M1-解耦验证/README.md`。
- 更新 `agents/reports/M1-Protocol-Agent-报告.md`。

## 验证结果

| 层级 | 项目 | 结果 | 说明 |
|---|---|---|---|
| L0 | 文件范围检查 | 通过 | 本轮仅新增或修改协议、测试资产、任务卡、报告、交付记录和工作台文档。 |
| L1 | protocol v0 字段一致性检查 | 通过 | 样例沿用 32 字节 header、big endian、`MADS`、`version = 0`、`VIDEO_CONFIG` JSON、H.264 Annex B。 |
| L1 | payload_len 样例长度检查 | 通过 | 使用 UTF-8 字节长度校验：HELLO 为 190 字节，VIDEO_CONFIG 为 163 字节。 |
| L2 | replay server 工具运行 | 未验证 | 本轮只定义约定，未实现工具代码。 |
| L2 | dump client 工具运行 | 未验证 | 本轮只定义约定，未实现工具代码。 |
| L2 | `sample-annexb.h264` 生成 | 未验证 | 已明确生成命令和验收方式，未实际生成二进制 fixture。 |
| L3 | Android MediaCodec 播放 fixture | 未验证 | 本轮不连接设备、不运行模拟器。 |
| L3 | Mac Host 输出被 dump client 接收 | 未验证 | 本轮不修改或运行 Mac Host。 |
| L4 | Mac/Android 端到端联调 | 不适用 | 解耦验证要求先完成两端独立验证。 |

## 未验证内容

- `sample-annexb.h264` 实际二进制文件。
- `protocol-frame-example.bin` 实际二进制文件。
- replay server 和 dump client 的工具代码。
- 小米平板 6 Pro 或 Android 模拟器上的 MediaCodec 行为。
- Mac VideoToolbox 输出到 Annex B 的实际转换链路。

## 范围确认

本轮没有改变 M1 架构、安全边界或依赖边界；没有修改 `src/mac-host/` 和 `src/android-client/` 实现代码；没有保存真实屏幕内容。

当前工作区存在其他来源的 `src/` 修改，本轮未处理、未回退、未纳入本交付结论。

## 下一步

- Protocol Agent 可继续生成无敏感内容的 `sample-annexb.h264` 与 `protocol-frame-example.bin`。
- Protocol Agent 可继续实现最小 replay server 和 dump client。
- Mac Host Agent 可按 `dump-client-notes.md` 独立验证。
- Android Client Agent 可按 `sample-annexb-generation.md` 和 `replay-server-notes.md` 独立验证。
