---
type: 任务卡
project: mac-android
status: 待人工验收
owner_agent: Codex (M1 Protocol Agent)
updated: 2026-06-30
---

# 当前任务：M1 Protocol 解耦验证资产约定

## 1. 用户原话

> 你现在就是Protocol Agent

选中目标：

> Protocol Agent 先跑目标：补齐 测试资产/M1-解耦验证/ 里的约定文件：protocol-frame-example.md replay-server-notes.md dump-client-notes.md 明确 sample-annexb.h264 生成方式

## 2. 本轮目标

- 补齐 `测试资产/M1-解耦验证/` 下的 protocol frame、replay server、dump client 约定。
- 明确 `sample-annexb.h264` 的生成方式、格式要求和验收方式。
- 更新 Protocol Agent 报告、验证交付和当前工作台。

明确不做：

- 不修改 Mac Host 实现。
- 不修改 Android Client 实现。
- 不生成真实屏幕内容。
- 不安装依赖、不连接设备、不运行模拟器。
- 不改变 M1 冻结技术选型。

## 3. 技术范围

- Protocol：补充 protocol v0 解耦验证资产约定。
- 测试资产：新增 `protocol-frame-example.md`、`replay-server-notes.md`、`dump-client-notes.md`、`sample-annexb-generation.md`。
- 文档：更新 `测试资产/M1-解耦验证/README.md`、Protocol Agent 报告、交付记录、当前工作台。
- 验证：执行 L0/L1 文档一致性检查。

## 4. 当前事实

- 已确认：M1 协议使用固定 32 字节 header、big endian、H.264 Annex B。
- 已确认：`VIDEO_CONFIG` 使用 UTF-8 JSON，不携带二进制 SPS/PPS。
- 已确认：关键帧 `VIDEO_FRAME` 必须设置 `FLAG_KEYFRAME`，携带 SPS/PPS 时设置 `FLAG_CONFIG`。
- 尚未验证：实际 replay server、dump client、fixture 二进制文件。

## 5. 操作边界

### 允许

- 修改 `测试资产/M1-解耦验证/`。
- 修改 `agents/reports/M1-Protocol-Agent-报告.md`。
- 修改协议相关交付记录。
- 更新本任务卡和 `当前工作台.md`。

### 禁止

- 修改 `src/mac-host/`。
- 修改 `src/android-client/`。
- 修改 M1 冻结技术选型。
- 保存真实屏幕内容作为测试资产。

## 6. 计划

1. 新增 protocol frame 样例说明。
2. 新增 replay server 约定。
3. 新增 dump client 约定。
4. 新增 sample Annex B 生成说明。
5. 更新测试资产 README、报告、交付记录和工作台。
6. 执行静态一致性检查。

## 7. 完成定义

- [x] protocol frame 样例说明已补齐。
- [x] replay server 约定已补齐。
- [x] dump client 约定已补齐。
- [x] `sample-annexb.h264` 生成方式已明确。
- [x] 测试资产 README 状态已更新。
- [x] 交付记录和报告已更新。
- [x] 未验证内容及原因已列明。

## 8. 验证要求

- 必须执行：L0 文件范围检查、L1 协议字段一致性检查。
- 不执行：构建、联网下载、设备连接、模拟器运行、真实屏幕采集。

## 9. 当前进度

- 当前状态：待人工验收。
- 已完成：测试资产约定文件、报告、交付记录、工作台更新、L0/L1 检查。
- 正在处理：无。
- 尚未处理：实际 fixture 和工具代码生成。
- 阻塞项：无。

## 10. 交接

- 最后可靠结论：本轮只固化解耦验证约定，不实现 Mac/Android 代码。
- 已修改文件：
  - `任务记录/2026-06-30-M1-Protocol-解耦验证资产约定.md`
  - `测试资产/M1-解耦验证/README.md`
  - `测试资产/M1-解耦验证/protocol-frame-example.md`
  - `测试资产/M1-解耦验证/replay-server-notes.md`
  - `测试资产/M1-解耦验证/dump-client-notes.md`
  - `测试资产/M1-解耦验证/sample-annexb-generation.md`
  - `agents/reports/M1-Protocol-Agent-报告.md`
  - `交付记录/2026-06-30-M1-Protocol-解耦验证资产约定-验证说明.md`
  - `当前工作台.md`
- 已执行命令：读取 AGENTS、M1 解耦验证策略、protocol-v0、测试资产 README、Agent 提示词；执行文件范围与协议字段一致性检查。
- 已获得证据：`测试资产/M1-解耦验证/` 已包含 replay server、dump client、protocol frame 和 sample Annex B 生成说明。
- 不确定事项：实际工具代码、`sample-annexb.h264` 和 `protocol-frame-example.bin` 尚未生成。
- 下一位 Agent 应先读取：`测试资产/M1-解耦验证/README.md`。
